# Mammogram inference app - Local and GCP App Engine (Python 3.12)
# Install: pip install -r requirements.txt
# Local: python main.py  (then open http://localhost:8080)
# GCP: gcloud app deploy

# Web app (local + GCP)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# ML inference and training
tensorflow>=2.12.0
numpy>=1.24.0
scikit-learn>=1.2.0
Pillow>=9.0.0

# Data loading and training (PrototypeA/B/C train.py, common/data_loader.py)
pandas>=1.5.0

# Evaluation and plotting (PrototypeA/B/C evaluate.py)
matplotlib>=3.6.0
seaborn>=0.12.0
scipy>=1.9.0
